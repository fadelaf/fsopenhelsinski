{
  "name": "backend",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start": "node --inspect index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../part2-notes-frontend && npm run build && cp -r dist ../part3-practice/",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push origin deploy-part3-practice",
    "lint": "eslint ."
  },
  "author": "Matti Luukkainen",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongoose": "^9.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@stylistic/eslint-plugin": "^5.6.1",
    "eslint": "^9.39.1",
    "globals": "^16.5.0"
  }
}
